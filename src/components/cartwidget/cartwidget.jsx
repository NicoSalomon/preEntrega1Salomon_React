import { Avatar, Badge } from 'antd';

const Cartwidget = () => {
    return (
        <Badge size="default" count={5}>
            <Avatar shape="square"/>
        </Badge>
    )
}

export default Cartwidget